module Liquid

  # Holds a conditional section, which is essentially just a string, but can be deferentiated from a regular string so they can be processed for nested conditionals
  class ConditionalSection < String

  end
end
